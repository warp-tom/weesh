import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App load smoke test', (WidgetTester tester) async {
    // Stub test to satisfy the Dart analyzer after flutter create generates the test directory.
    expect(true, isTrue);
  });
}
